interface Props {
  title: string;
  src: string;
  artist: string;
  // thumbnail: string; (if we can find it :sob:)
}

const Song = (props: Props) => {
  return <div></div>;
};

export default Song;
