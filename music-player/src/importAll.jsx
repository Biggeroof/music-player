
const importAll = () => {
    function importAll(require) {
        console.log(r);
        console.log(1)
        let files = {};
        r.keys().map((item, index) => { images[item.replace('./', '')] = r(item); });
        return files;
    }
}

export default importAll